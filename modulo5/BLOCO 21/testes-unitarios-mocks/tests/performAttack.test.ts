
describe("test performAttack", () =>{
    test("Showing jest.fn", () => {
        const mock = jest.fn(() => {
            
            return true
        })
    })

    test("Showing jest.fn", () => {
        const mock = jest.fn(() => {
            
            return false
        })
    })
})